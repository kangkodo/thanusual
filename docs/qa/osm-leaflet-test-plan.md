# Test Plan

Generated by /plan-eng-review on 2026-09-03
Branch: feat/osm-leaflet-map
Repo: kangkodo/thanusual

## Affected Pages/Routes

- `/` (thanusual.pages.dev or wrangler pages dev) — list is default; map is a second view

## Key Interactions to Verify

- Category tabs still filter the list
- 보기 목록 / 지도 does not replace category tabs
- Opening 지도 after load shows Seoul circles sized by grade
- Clicking a row opens the map at zoom 14 with that place selected
- Clicking a 붐빔 pin returns to the list and scrolls that row into view
- Skip link target is `#board` on list and `#map` on map

## Edge Cases

- Data load failure: 지도 button disabled, list empty copy unchanged
- Missing `CARTO_API_KEY` locally: overlays still show on gray; status line on the map
- Category 공원 on the map draws only park rows
- `warming` banner still appears; map height still fills the rest

## Critical Paths

- List ranking order unchanged vs production
- No osm.org tile requests in the network panel
- No API key in the JS bundle
