# 개발 TODO — 관심 장소 고정

문구는 기획만, 체크와 완료 근거는 개발만 바꾼다.

- [ ] DEV-001 `lib/pins.js`를 만든다. 브라우저 API에 직접 기대지 않는 순수 함수만 둔다.
  - `PIN_KEY = "thanusual.pins.v1"`
  - `readPins(storage)` → `Set<string>`. `storage`가 없거나, `getItem`이 예외를 던지거나, 값이 JSON이 아니거나 배열이 아니면 빈 Set. 배열 안의 문자열이 아닌 값은 버린다.
  - `writePins(storage, pins)` → 성공하면 `true`, 실패(저장소 없음·예외)하면 `false`. 절대 예외를 던지지 않는다. 저장 형식은 이름 문자열의 JSON 배열.
  - `togglePin(pins, name)` → 새 Set을 돌려준다. 입력 Set은 바꾸지 않는다.
  - `pinnedRows(data, pins)` → `visibleRows(data, "전체", "")` 결과 중 `pins`에 있는 장소만, 같은 순서로. 분류 탭·검색어와 무관(A-1). 데이터에 없거나 `fresh`가 아닌 장소는 결과에 없다(아래 목록과 같은 규칙, R-6).
  확인 방법: AC-1~AC-6
- [ ] DEV-002 `shared.js`의 `state`에 `pins: new Set()`을 추가하고, `app.js` 시작 시 `readPins(localStorage 접근을 try로 감싼 값)`으로 채운다. `localStorage` 접근 자체가 예외를 던지는 환경에서도 시작이 실패하지 않는다(R-5).
  확인 방법: AC-3, QA-6
- [ ] DEV-003 `index.html`의 `#board` 바로 위에 고정 구역을 둔다: `<section id="pinned" aria-labelledby="pinned-title" hidden>`, 제목 `<h2 id="pinned-title">고정한 장소</h2>`, 목록 `<ul class="board pinned-list" id="pinned-list">`. 고정한 장소가 0개(또는 `pinnedRows`가 빈 배열)면 `hidden`.
  확인 방법: QA-1, QA-2
- [ ] DEV-004 `app.js`의 행 그리기를 함수로 빼서 두 목록이 같이 쓴다. 아래 순위 목록은 지금처럼 순위 번호를 보이고, 고정 구역 행은 순위 번호 자리에 `★`를 보인다(A-2). 순위 목록의 번호와 순서는 바뀌지 않는다.
  확인 방법: QA-3
- [ ] DEV-005 각 행의 `<li>` 안, `.row-btn` **옆**(안이 아님)에 고정 버튼 `<button type="button" class="pin-btn">`를 둔다. `aria-pressed`로 고정 여부, `aria-label`은 고정 전 "`{장소} 고정`", 고정 후 "`{장소} 고정 해제`". 글자는 ☆/★. 누르면 `togglePin` → `writePins` → 다시 그리기. 이 클릭은 행 선택·지도 이동·휴대폰 시트 닫기를 일으키지 않는다(기존 `#board` 클릭 처리기가 `.pin-btn`을 먼저 걸러낸다). 두 목록 모두 같은 처리.
  확인 방법: QA-4, QA-5
- [ ] DEV-006 다시 그린 뒤 키보드 포커스: 고정 버튼을 눌렀다면 **같은 장소의 고정 버튼**으로 포커스를 돌려준다. 해제로 고정 구역에서 그 행이 사라지면 아래 순위 목록의 같은 장소 고정 버튼으로 간다. 기존 "새로고침 때 행 포커스 유지" 동작은 그대로.
  확인 방법: QA-5
- [ ] DEV-007 장소 상세(`#place-detail`)의 제목 옆에 같은 고정 버튼(같은 `aria-*` 규칙)을 둔다. 누르면 목록과 상세가 함께 갱신된다.
  확인 방법: QA-7
- [ ] DEV-008 `styles.css`: 고정 버튼은 누를 수 있는 영역이 44×44px 이상, 기존 색 토큰만 사용(새 색 없음), 다크 모드에서도 대비 유지. 고정 구역 제목은 기존 섹션 제목 스타일을 따른다. 휴대폰 바텀시트 안에서 레이아웃이 깨지지 않는다.
  확인 방법: QA-8

## 수용 기준

| ID | 기준 | 확인 방식 |
|---|---|---|
| AC-1 | `togglePin`은 추가·해제가 되고 입력 Set을 바꾸지 않는다 | 자동: `tests/acceptance/pins.test.js` |
| AC-2 | `writePins` 후 `readPins`로 같은 이름들이 돌아온다(키 `thanusual.pins.v1`) | 자동 |
| AC-3 | 저장소가 없거나 예외를 던져도 `readPins`는 빈 Set, `writePins`는 `false`를 돌려주고 예외가 없다 | 자동 |
| AC-4 | 깨진 저장 값(JSON 아님, 배열 아님, 문자열 아닌 원소)을 안전하게 거른다 | 자동 |
| AC-5 | `pinnedRows`는 데이터에 없거나 `fresh`가 아닌 이름을 무시한다 | 자동 |
| AC-6 | `pinnedRows` 순서는 `visibleRows(data, "전체", "")`와 같고, warming 전·후 모두 성립한다 | 자동 |
| QA-1 | 고정 0개일 때 고정 구역이 보이지 않는다 | 관찰: 첫 방문 화면 |
| QA-2 | 장소 하나를 고정하면 목록 맨 위 "고정한 장소"에 나타나고, 분류 탭을 바꾸거나 검색해도 계속 보인다 | 관찰 |
| QA-3 | 아래 순위 목록의 순위 번호·순서가 고정 전후로 같다 | 관찰 |
| QA-4 | ☆를 눌러도 장소가 선택되거나 지도가 움직이지 않는다. 행 본문을 누르면 기존처럼 선택된다 | 관찰 |
| QA-5 | 키보드(Tab·Enter/Space)로 고정·해제할 수 있고, 누른 뒤 포커스가 같은 장소의 고정 버튼에 남는다. `aria-pressed`가 바뀐다 | 관찰 |
| QA-6 | 새로고침 후 고정이 유지된다. 저장소를 막은 상태에서도 페이지가 뜨고 고정이 그 탭에서 동작한다 | 관찰 |
| QA-7 | 상세 패널의 고정 버튼과 목록의 고정 상태가 서로 맞는다 | 관찰 |
| QA-8 | 휴대폰 폭(375px)에서 바텀시트 안 고정 구역과 버튼이 깨지지 않는다. 다크 모드 대비 유지 | 관찰: 캡처 |

## 영향 문서
- `CLAUDE.md` "First screen" 단락: 목록 위 고정 구역과 저장 방식(이 브라우저에만) 한 문장 추가
- `README.md`: 갱신 불필요. 사용자용 설명이 데이터 산출 규칙 중심이고 UI 조작을 다루지 않는다

## 하지 않을 일
- `map.js` 수정, 지도 핀 모양 변경
- 서버 저장, 계정, 공유 링크(`#p=`)에 고정 목록 담기
- 금지 경로 수정(`package.json` 포함). 새 의존성 없음
- 고정 개수 제한(A-3)
